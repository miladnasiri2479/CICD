pipeline {
  agent any

  stages {
    stage('develop') {
      steps {
         echo "this project depeloved"
         echo "its OK"
      }
    }

    stage('test') {
      steps {
        echo "project tested"
      }
    }

    stage('deploy') {
      steps {
         echo "this is step deploy in production"
      }
    }
  }
}
